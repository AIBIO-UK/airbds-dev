name: Build Assessment Skill for Test

# Trigger the workflow when changes are pushed to the main branch
on:
  push:
    branches:
      - main
      - skills-testing
    paths:
      - 'skills/testing/airbds-assessment-skill/SKILL.md'
      - 'skills/testing/airbds-assessment-skill/templates/*'
  workflow_dispatch:

jobs:
  latest-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create ZIP file
        run: |
          # Zip from inside the skill directory so its structure (including
          # templates/) is preserved with the skill dir as the zip root. No -y,
          # so the symlinked metric template is dereferenced into a real file.
          ( cd skills/testing/airbds-assessment-skill && zip -r - . ) > airbds-assessment-skill.zip

      - name: Delete previous release and tag
        # softprops/action-gh-release updates an existing release's assets but
        # leaves the git tag and publish date on the old commit, so the release
        # page looks stale even though the zip was rebuilt. Delete the previous
        # release + tag first so the next step recreates them at this commit
        # with a fresh date. The download URL (tag + filename) is unchanged.
        # `|| true` covers the first run, when no release exists yet.
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release delete assessment-skill-testing --cleanup-tag --yes || true

      - name: Update Test Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: assessment-skill-testing
          name: "AIRBDS assessment testing skill zip"
          draft: false
          prerelease: true
          files: airbds-assessment-skill.zip
